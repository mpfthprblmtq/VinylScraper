# class used to store the twitter user's handle and the keyword to search on
class TwitterProfile:
    def __init__(self, handle, name, keywords):
        self.handle = handle
        self.name = name
        self.keywords = keywords
